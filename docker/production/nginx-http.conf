# Included into nginx's http {} block. The zone must be declared here, not in the
# server block, because limit_req_zone is only valid at http level.
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;
